# Jobthai plus frontend #

## Install dependency ##

`npm install` or `yarn`

## Start project in development mode ##

`npm run dev` or `yarn dev`

## Start project in production mode ##

`npm run build` or `yarn build`

`npm start` or `yarn start`